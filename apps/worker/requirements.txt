# =============================================================================
# Lumio Worker — Python Dependencies
# =============================================================================

# Task Queue
celery[redis]==5.4.0
redis==5.1.1

# Image processing
pyvips==2.2.3       # libvips bindings — sehr schnelle Image-Pipeline (auch
                    # für HEIC via libheif, das libvips zur Build-Zeit linkt)
Pillow==11.0.0      # Fallback und einfache Operationen
rawpy==0.24.0       # LibRaw bindings — RAW-Decoding (CR2/CR3/NEF/ARW/...)
imageio==2.36.0     # Bridge zwischen rawpy und Pillow/pyvips

# Video processing — via subprocess zu ffmpeg, das im Container installiert ist
# (keine Python-Bindings nötig)

# S3-Client
boto3==1.35.49

# DB-Updates (Worker schreibt File-Status zurück)
psycopg[binary]==3.2.3
sqlalchemy==2.0.36

# Hilfsfunktionen
python-magic==0.4.27     # Mime-Type-Detection
zipstream-ng==1.8.0      # Streaming-ZIP-Builder
# EXIF-Reading läuft über exiftool (im Image installiert) statt pyexiv2 —
# exiftool ist Multi-Arch (amd64 + arm64), pyexiv2 lieferte nur ein
# x86_64-Wheel und blockierte damit ARM-Builds. Siehe exif_meta.py.
structlog==24.4.0
python-dotenv==1.0.1

# Test
pytest==8.3.3
