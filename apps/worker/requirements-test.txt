# =============================================================================
# Lumio Worker — Test Dependencies
# =============================================================================
# Wird zusätzlich zu requirements.txt installiert:
#   pip install -r requirements.txt -r requirements-test.txt
# =============================================================================

testcontainers[postgres,minio]==4.8.2
docker>=7.0.0
boto3>=1.35.0
psycopg[binary]>=3.2.0
Pillow>=11.0.0      # für Fixture-Erzeugung in conftest
pytest>=8.3.0
